[project]
name = "image-vision-mcp"
version = "0.1.0"
description = "MCP server for image description via Doubao Vision API"
requires-python = ">=3.11"
dependencies = [
    "mcp>=1.0.0",
    "httpx>=0.27.0",
]

[project.scripts]
image-vision-mcp = "server:main"
